<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.microsoft.mapper.EmpMapper">
    <!-- 根据传递过来的被封装的查询条件对象 -->
    <select id="list" resultType="com.microsoft.pojo.Emp">
        <!-- 与Emp对象中deptName字段名匹配 -->
        select e.*, d.name as deptName
        from emp e
        left join dept d on e.dept_id = d.id
        <!-- 根据条件中是否对名字、性别、入职日期进行限定动态指定查询条件 并且自动控制 and or 关键字添加 -->
        <where>
            <if test="name != null and name != ''">
                e.name like concat('%', #{name}, '%')
            </if>
            <if test="gender != null">
                and e.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and e.entry_time between #{begin} and #{end}
            </if>
        </where>
        order by e.update_time desc
    </select>
    <!-- 根据id列表批量删除员工 -->
    <delete id="delete">
        delete from emp where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <!-- 查询回显 -->
    <resultMap id="empResultMap" type="com.microsoft.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="job" property="job"/>
        <result column="salary" property="salary"/>
        <result column="entry_time" property="entryTime"/>
        <result column="image" property="image"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="password" property="password"/>
        <result column="dept_id" property="deptId"/>
        <!-- 封装员工的工作经历 -->
        <collection property="exprList" ofType="com.microsoft.pojo.EmpExpr">
            <id column="ee_id" property="id"/>
            <result column="ee_begin" property="begin"/>
            <result column="ee_end" property="end"/>
            <result column="ee_company" property="company"/>
            <result column="ee_job" property="job"/>
            <result column="ee_emp_id" property="empId"/>
        </collection>
    </resultMap>
    <select id="getInfoById" resultMap="empResultMap">
        select e.*,
               ee.id      ee_id,
               ee.begin   ee_begin,
               ee.end     ee_end,
               ee.job     ee_job,
               ee.company ee_company,
               ee.emp_id  ee_emp_id
        from emp e
                 left join emp_expr ee on e.id = ee.emp_id
        where e.id = #{id}
    </select>
    <!-- 根据id更改员工基本信息 -->
    <update id="update">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="entryTime != null">entry_time = #{entryTime},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="deptId != null">dept_id = #{deptId}</if>
        </set>
        where id = #{id}
    </update>
    <!-- 查询员工职位数量 -->
    <select id="countEmpJobData" resultType="java.util.Map">
        select count(*)              as num,
               (case
                    when job = 1 then '班主任'
                    when job = 2 then '讲师'
                    when job = 3 then '学工主管'
                    when job = 4 then '教研主管'
                    when job = 5 then '咨询师'
                    else '其他' end) as pos
        from emp
        group by job
        order by num;
    </select>
    <!-- 员工性别统计 -->
    <select id="countEmpGenderData" resultType="java.util.Map">
        select count(*) as value, if(gender = 1, '男性员工', '女性员工') as name from emp group by gender;
    </select>
    <!-- 查询所有员工 -->
    <select id="getAllEmp" resultType="com.microsoft.pojo.Emp">
        select * from emp
    </select>
</mapper>